plugins {
    id 'java'
}

compileJava.options.encoding = 'UTF-8'

tasks.withType(JavaCompile) {
    options.encoding = 'UTF-8'
}
description = "cryptocurrency analysis and trading"

allprojects {
    group 'io.runon.cryptocurrency'
    repositories
            {
                mavenCentral()
            }
}